"use client";

type ButtonProps = {
  variant?: "primary" | "secondary" | "outline";
  size?: "sm" | "md" | "lg";
  disabled?: boolean;
};

const Button = () => {
  return <div>Button</div>;
};
export default Button;
